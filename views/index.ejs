<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SFT App</title>
</head>
<body>
    <form action="/upload" method="post" style="display: grid;gap:.5rem;
    grid-template-columns: auto 1fr;max-width: 500px; margin: 0 auto;" enctype="multipart/form-data">
        <label for="file">File:</label>
        <input type="file" id="file" name="file" required />
        <label for="password">password:</label>
        <input type="password" id="password" name="password" />
        <button style="grid-column: span 2;" type="submit">share</button>
    </form>
    <% if (locals.fileLink != null){ %>
        <div style="margin-top: 1rem;">
            Your file is Uploaded
            <a href="<%= locals.fileLink %> "><%= locals.fileLink %></a>
        </div>
        <% } %> 
</body>
</html>